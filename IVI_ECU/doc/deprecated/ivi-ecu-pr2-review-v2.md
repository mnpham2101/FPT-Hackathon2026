# IVI ECU — PR #2 review, v2

> **Version:** v2 — 2026-08-06. Re-review of pull request #2 after its merge to `main`.
> **Pull request:** #2 — branch `feat/phase5-ivi-hmi-dev` → `main` (title not read; `gh` remains unauthenticated, so the PR body and its check runs were not consulted)
> **Reviewed commit:** `24fa852` — the merge of PR #2 onto `main`, branch tip `723c7a8`. No IVI commit has landed after it, so this is also a review of the node's current state on `main`.
> **Base:** the merge's first parent `f7bbd03`; diff range `f7bbd03..24fa852` — 60 files, +6181 / −113.
> **Judged against:** [ivi-ecu-hld.md](../../../documents/Design/IVI-ECU/ivi-ecu-hld.md) and [ivi-ecu-design-decisions.md](../../../documents/Design/IVI-ECU/ivi-ecu-design-decisions.md) · [m1-cooperative-awareness.md §2](../../../requirements/m1-cooperative-awareness.md) · [milestone1_high_level_plan.md § Phase 5](../../../documents/Plan-Proposal/milestone1_high_level_plan.md) and [phase5_minh_tasks.md](../../../plans/phase5_minh_tasks.md), the authoritative Phase 5 plan · the frozen schemas [r4-ada-ivi.schema.json](../../contracts/r4-ada-ivi.schema.json) and [r3-tracked-object.schema.json](../../contracts/r3-tracked-object.schema.json) · [deploy-ivi-hmi-walkthrough.md](../../../requirements/car-sky-guide/deploy-ivi-hmi-walkthrough.md) for deployment-facing claims.
> **Previous version:** [ivi-ecu-pr2-review-v1.md](ivi-ecu-pr2-review-v1.md), committed as `40687ad`, judged branch commit `f7f2f55`.
> **Verified:** none of the build or test results below were reproduced on this host — the only JDK carrying a compiler here is 25.0.2, which Gradle 8.13 refuses to run under, and the default Java is a compiler-less JRE 21. Test and build status therefore comes from what the repo records: `phase5-ci.yml` exists on `main` with the `ivi-unit-tests` and `ivi-assemble` lanes, the test tree carries 60 `@Test` methods across 11 files, and [phase5_vinh_progress.md](../../../plans/phase5_vinh_progress.md) and the branch's completion report claim green — but **no CI run ID for either IVI lane is recorded anywhere in the repo**, so "the tests pass" is a recorded claim, not a verified fact of this review. No behaviour in a deployed Room is verified: the repo records no install, launch, or logcat evidence from the AAOS node.

## Since v1

**Fixed — and fixed well.** Four of v1's hardest findings were answered by commits that name them, and the answers are correct:

| v1 finding | Fix on this branch | Where it now stands |
|---|---|---|
| No launchable component; nothing constructs or connects anything | `79f6e66`, `7520804` | [MainActivity.kt](../../app/src/main/java/com/hackathon/v2x/ivi/MainActivity.kt) starts and binds `R4ListenerService`, calls `attachService`, and mounts `MainScreen`; the manifest has a LAUNCHER activity. The datagram→screen path exists at runtime |
| Buffer reuse truncates every datagram after the first | `7c48b1b` | `packet.setLength(buffer.size)` before every receive ([R4ListenerService.kt:135](../../app/src/main/java/com/hackathon/v2x/ivi/service/R4ListenerService.kt#L135)); the socket is now also closed in `onDestroy` so cancellation unblocks the blocking receive |
| Parser rewrites unknown `warningType` to `"unknown"` | `3cdbc6b` | The wire value is preserved verbatim ([R4Deserializer.kt:53-60](../../app/src/main/java/com/hackathon/v2x/ivi/data/R4Deserializer.kt#L53-L60)) — D4's parser half |
| `R4ServiceError` added a third member to the frozen two-member R4 hierarchy | `6542211` | It is no longer an `R4Message` subtype and no longer `@Serializable` ([R4Message.kt:58-69](../../app/src/main/java/com/hackathon/v2x/ivi/model/R4Message.kt#L58-L69)) — the drift is undone |
| Listener on port 5004 while the deployed ADA sender targets 47300 | `06cb936` | `R4_UDP_PORT` defaults to **47300** ([build.gradle.kts:37](../../app/build.gradle.kts#L37)); the mock sender agrees |
| Producer never fills `vehicleCSnapshot`, so the R19 guard reads `null` and passes C as trusted | `8203714` | `WarningViewModel` copies the message's `object` into `SceneGeometry.vehicleCSnapshot` ([WarningViewModel.kt:73](../../app/src/main/java/com/hackathon/v2x/ivi/ui/WarningViewModel.kt#L73)) — D12's obligation, met |
| A successful receive writes no log line | `14feee1` | `[RX]` and `[DROP]` lines on the `IVI_V2X` tag ([R4ListenerService.kt:140-143](../../app/src/main/java/com/hackathon/v2x/ivi/service/R4ListenerService.kt#L140-L143)) |
| No CI | `31fc214` and main-side work | `phase5-ci.yml` carries `ivi-unit-tests` and `ivi-assemble`, uploading `app-debug-apk` under the walkthrough's artifact name |
| Mock sender omits `class` and mis-shapes `vehicles` | `f72d8a6` | The **warning** payload is now schema-correct field for field. The **state** payload is not — see below |

Wake-on-warning also now exists and is tested: `MainViewModel` forces `WarningView` on Active, restores `previousMode` on Idle, and respects a user override ([MainViewModel.kt:61-79](../../app/src/main/java/com/hackathon/v2x/ivi/ui/MainViewModel.kt#L61-L79)). The test suite grew from 15 to 60 tests, including a genuine full-stack loopback test — real UDP through the real service to both view-models ([FullStackIntegrationTest.kt:93-123](../../app/src/test/java/com/hackathon/v2x/ivi/integration/FullStackIntegrationTest.kt#L93-L123)).

**Still standing from v1:**

- One Gradle module instead of five — `settings.gradle.kts` includes `:app` only; `:contract`, `:serializer`, `:observer` and `:r4-simulator` do not exist, and the socket, loop and back-off still live inside the Android service.
- `R3Snapshot` still gives defaults to 6 of 9 `required` R3 fields, and `R3Timestamps` defaults all three ([R3Snapshot.kt:20-27](../../app/src/main/java/com/hackathon/v2x/ivi/model/R3Snapshot.kt#L20-L27), [35-41](../../app/src/main/java/com/hackathon/v2x/ivi/model/R3Snapshot.kt#L35-L41)). The mock that motivated them is fixed; the drift remains.
- `WarningClassifier`, `AndroidR4Logger`/`R4Logger`, `IviRuntimeConfig`, `DevInjectorReceiver` and `R4Contract` are still absent; `schemaVersion` is still read by no code; the `[LINK]` and `[UI]` evidence lines still do not exist.
- Hilt instead of D7's manual `IviGraph` — now active rather than inert, at exactly the costs D7 predicted: a Guava runtime patch (`f93fc19`) and a hand-written factory because Hilt cannot inject one view-model into another ([MainActivity.kt:91-101](../../app/src/main/java/com/hackathon/v2x/ivi/MainActivity.kt#L91-L101)).
- The state path: the mock's heartbeat sends `{position, speed}` objects where `R4Vehicles` binds bare `{x, y}` ([mock_r4_sender.py:69-71](../../mock-sender/mock_r4_sender.py#L69-L71) against the schema's `stateMessage`), so every mock state message still fails to decode; the repository's `currentState` collector is still an empty body ([WarningViewModel.kt:60-64](../../app/src/main/java/com/hackathon/v2x/ivi/ui/WarningViewModel.kt#L60-L64)); the `VehicleState` orphan — the visible seam of that disagreement — is still declared and used by nothing ([R4Message.kt:71-76](../../app/src/main/java/com/hackathon/v2x/ivi/model/R4Message.kt#L71-L76)).
- D13 is still not implemented: any warning, `low` included, raises Idle → Active ([WarningViewModel.kt:67-75](../../app/src/main/java/com/hackathon/v2x/ivi/ui/WarningViewModel.kt#L67-L75)); D13 requires a `low` to update the scene without raising the view.
- `WarningUiState.Error` is still unreachable — the service exposes `serviceError`, nothing collects it, and `onServiceError()` has no caller.
- `mock-sender/` is still in `IVI_ECU/` though it shares nothing with the Gradle build — [node-code-layout.md § tools/](../../../.claude/rules/node-code-layout.md) sends it to `tools/`; the designed `r4-simulator/` (scenario files as data, image `m1-r4-sim:latest`) does not exist, and the designated `r4-sim-image` CI lane is absent even though `phase5-ci.yml`'s own header names it.
- No deployment evidence: nothing in the repo records the APK installed, launched or receiving on the AAOS node.

**Newly broken.** The largest new findings are in §1 and §2; in one line each:

- A **fabricated scene stamped `v2x_relayed`** renders whenever the Warning view is shown with no live warning ([MainScreen.kt:255-277](../../app/src/main/java/com/hackathon/v2x/ivi/ui/screen/MainScreen.kt#L255-L277)).
- The **view seam is bypassed** — the injected `IviWarningViewSeam` is ignored and both renderers are constructed directly ([MainScreen.kt:282-287](../../app/src/main/java/com/hackathon/v2x/ivi/ui/screen/MainScreen.kt#L282-L287)).
- `WarningBannerOverlay` is **mounted** ([MainScreen.kt:291-295](../../app/src/main/java/com/hackathon/v2x/ivi/ui/screen/MainScreen.kt#L291-L295)), against D11 and R17's "no banner".
- The default renderer is a new **3D view that draws the annotated figure** R17 says is never rendered, at fixed canvas anchors that ignore the message's positions.
- A **second HLD** (`phase5-ivi-ecu-hld.md`), a `deployment/` procedure folder, and four more Phase 5 plan files landed on `main`, each a competing authority to one this review judges by.
- The shared **`verify-arm64-image` action was weakened** from by-digest-only to a mutable-tag fallback (`3c0e518`), and a machine-specific `.vscode/settings.json` with absolute `C:\Program Files\…` paths was committed at the repo root.

---

## 1 · Architecture review

Rows are the components [ivi-ecu-hld.md §3/§6](../../../documents/Design/IVI-ECU/ivi-ecu-hld.md#3-the-component-architecture) defines, at the paths §4 designates. A designed component the branch never created is a row at a low percentage, not an omission. The second HLD the branch merged (`phase5-ivi-ecu-hld.md`) is **not** an authority here — [hld-content-and-commit-format.md](../../../.claude/rules/hld-content-and-commit-format.md) fixes one HLD per node, and `ivi-ecu-hld.md` §1 names itself the sole design authority; the second document's existence is itself a finding (its own header even says "Commit deferred — no `[X] design:` commit until explicitly requested", yet it was committed, untagged).

| Module in design | % complete | Abides by the architecture? Relations to other components |
|---|---|---|
| **`:contract`** — `model/`, `R4Json`, `R4Contract` *(Data)* | **60%** | **Partly.** Still a package in `:app`, not a module, so nothing else can share it. `R4ServiceError` is off the wire hierarchy again — the worst drift is undone. Remaining: the R3 required-field defaults (above), the `VehicleState` orphan, and no `R4Contract.kt`, so `KNOWN_SCHEMA_VERSION` has nowhere to live and the version half of additive evolution has no anchor. |
| **`:serializer/R4Deserializer`** *(Business logic)* | **60%** | **Partly.** Preserves `warningType` (D4) and returns `Result` — the two judgements that matter most are now both right. Still in `:app/data/` with an `android.util.Log` import; still `ByteArray` rather than the designed `(buffer, offset, length)` slice; no `R4DecodeResult`, no BOM strip, no `schemaVersionAhead`. The M1 registry is still a hardcoded boolean in the parser ([R4Deserializer.kt:70-71](../../app/src/main/java/com/hackathon/v2x/ivi/data/R4Deserializer.kt#L70-L71)), now used only for a log line. |
| **`:observer/JdkDatagramSource`** *(Business logic)* | **20%** | **No.** The component does not exist; the socket is opened inline in the service. Its two designed behaviours are now present *inline*: length reset each receive, close on destroy. The seam (`R4DatagramSource`) still does not exist, which is why only the Robolectric integration test can reach the loop. |
| **`:observer/R4SocketObserver`** *(Business logic)* | **45%** | **Partly, and inline.** Back-off, `[RX]` and `[DROP]` exist. Missing: the truncation-suspect check (`BUFFER_SIZE = 4096`, no `length == buffer.size` test), `R4LinkState` and the `[LINK]` line, and the bounded `DROP_OLDEST`/`tryEmit` policy of D5 — `emit` on a full 64-slot buffer suspends, so a slow collector can still stall the socket. |
| **`warning/WarningClassifier`** *(Business logic)* | **10%** | **Absent.** Presentation mapping is inlined in each renderer's `riskColor`, with unknown risk failing safe to high — a good instinct in the wrong place, duplicated across two files ([CanvasWarningView.kt:112-117](../../app/src/main/java/com/hackathon/v2x/ivi/ui/view/CanvasWarningView.kt#L112-L117), [Canvas3DWarningView.kt:86-90](../../app/src/main/java/com/hackathon/v2x/ivi/ui/view/Canvas3DWarningView.kt#L86-L90)). |
| **`ui/view/SceneCoordinateMapper`** *(Business logic)* | **100%** | Correct, tested — and used only by the 2D renderer. The default 3D renderer bypasses it entirely (next section). |
| **`data/R4Repository`** *(Data)* | **75%** | **Yes, and now attached.** Routes `warning` → `SharedFlow`, `state` → last-value `StateFlow`, zero Android imports, tested, and `attachToService` is called at runtime via the activity's service binding. Missing: link state, the transport-error route (it drops the topic entirely now that `R4ServiceError` is out of the flow's type), and the dev-injector entry point. |
| **`ui/WarningViewModel`** *(UI logic)* | **70%** | **Right layer, mostly right behaviour.** Idle ↔ Active with `WARNING_TIMEOUT_MS`, and the D12 snapshot fill — the R19-critical line — is here and regression-relevant. Not implemented: D13 (`low` raises the view like any other risk), the state collector (empty body), and `Error` (uncallable). |
| **`ui/MainViewModel` + `ui/DisplayMode`** *(UI logic)* | **85%** | **Yes.** Wake-on-warning, previous-mode restore, user-override — implemented and tested (6 tests). The `DisplayMode` set is exactly the sanctioned four. Missing only the `[UI] mode=… cause=…` evidence line, so no observer can tell *why* the mode changed. |
| **`config/IviRuntimeConfig`** *(UI logic)* | **35%** | **Absent, defaults improved.** `R4_UDP_PORT=47300` and `WARNING_TIMEOUT_MS` are `BuildConfig` fields — no literals in logic, and the port now matches the blueprint. But D10's launch-time intent-extra merge point does not exist, so an installed APK still cannot be reconfigured without a rebuild, and the other D10 keys (buffer, flow capacity, back-off, scene scale) are ad-hoc constants in the service. |
| **`ui/screen/MainScreen`** *(UI)* | **75%** | **Mounted and real, with three defects.** The R16 layout runs, the side buttons switch the Display Area, `HomeView` is the default. Defects: (1) `defaultMockScene` — a fabricated ghost-C scene whose snapshot claims `source="v2x_relayed"` and whose risk defaults to `"high"`, rendered whenever the Warning view is open with no live data, reachable any time via the "3D HUD" side button ([MainScreen.kt:253-277](../../app/src/main/java/com/hackathon/v2x/ivi/ui/screen/MainScreen.kt#L253-L277)) — hardcoded scene literals against principle 5, and a display that can show a relayed warning **no message produced**, which is the one image the R19 evidence chain cannot afford; (2) the seam bypass below; (3) the status bar hardcodes `"BOUND :<port>"` regardless of whether the socket ever bound ([MainScreen.kt:144](../../app/src/main/java/com/hackathon/v2x/ivi/ui/screen/MainScreen.kt#L144)). |
| **`ui/view/IviWarningViewSeam`** *(UI)* | **60%** | **Exists, is realized twice, and is bypassed by its only consumer.** `AppModule` binds it to `Canvas3DWarningView`, `MainActivity` injects it and passes it down — and `WarningViewContent` ignores the parameter, constructing `Canvas3DWarningView()` and `CanvasWarningView()` directly with a local toggle ([MainScreen.kt:281-287](../../app/src/main/java/com/hackathon/v2x/ivi/ui/screen/MainScreen.kt#L281-L287)). `FullStackIntegrationTest` asserts the Hilt binding the runtime never uses ([FullStackIntegrationTest.kt:84-90](../../app/src/test/java/com/hackathon/v2x/ivi/integration/FullStackIntegrationTest.kt#L84-L90)) — a green test over a dead wire. |
| **`ui/view/CanvasWarningView`** + provenance guard *(UI)* | **85%** | The committed 2D renderer, guard live and fed a real snapshot at last. Deviations: it draws `d_AB`/`d_AC` labels and the `[V2X]` badge, which R17's "nothing else is drawn" and this HLD's own row exclude; and it is reachable only through the in-canvas 2D/3D toggle, not as the default. |
| **`ui/view/WarningBannerOverlay`** *(UI)* | built, **mounted in violation of D11** | The component is fine; mounting it ([MainScreen.kt:291-295](../../app/src/main/java/com/hackathon/v2x/ivi/ui/screen/MainScreen.kt#L291-L295), commit `56bc897`) contradicts D11 — a standing decision recording the user's own call — and R17's "no banner". It also shows for the fabricated mock scene, banner and all. |
| **`MainActivity` + `IviApplication` + `di/IviGraph`** *(Host)* | **80%** | **Exists and connects everything — the v1 gating finding is closed.** Composition is Hilt (`@HiltAndroidApp`, `@AndroidEntryPoint`, `AppModule`), not D7's `IviGraph`; the departure is now load-bearing and paid for (Guava patch, manual `MainViewModelFactory`). A decision is revisited by changing its entry, not by an implementation that departs from it — either D7 changes or this does. |
| **`service/R4ListenerService`** *(Host)* | **75%** | **Sound host, still holding three components' work.** Foreground, bound, started by the activity, socket closed on destroy, truncation reset in place. The layer collapse (socket + loop + parse dispatch inside an Android service) is the same as v1; it is now mitigated rather than fixed. |
| **`service/AndroidR4Logger`** *(Host)* | **25%** | **Absent as a component.** The `IVI_V2X` tag now exists ad hoc: `[RX]`/`[DROP]` in the service, the guard's ERROR in the 2D view. No `R4Logger` seam, no `key=value` structure, no `[LINK]`/`[UI]` — and the **3D renderer logs its guard ERROR on `IVI_V2X_GODVIEW`** ([Canvas3DWarningView.kt:92](../../app/src/main/java/com/hackathon/v2x/ivi/ui/view/Canvas3DWarningView.kt#L92)), which `adb logcat -s IVI_V2X` — the command every walkthrough and the node's own deploy note cite — does not show. |
| **`r4-simulator/` → `m1-r4-sim:latest`** *(Test equipment)* | **35%** | **Still `mock-sender/`, still misplaced, half on-contract.** The warning payload now round-trips (verified by reading it against the schema). The state payload does not (`{position, speed}` vs `{x, y}`). Scenarios are code branches, not D9's data files; no image lane builds its Dockerfile; and sharing nothing with the Gradle build, it fails all four conditions of the consumer-folder exception — its home is `tools/`. |
| **`debug/DevInjectorReceiver`** *(Test equipment)* | **0%** | Absent. With the seam bypass above, the only no-network path to the UI is the fabricated mock scene — which is presumably why it exists. |
| **`Canvas3DWarningView` + `Scene3DProjection` + `HomeScreen`** *(not in the design)* | — | **Unsanctioned, and the 3D view is the runtime default.** R17 admits an optional 3D renderer *through the seam*; this one arrived without a design change, was made the default (`AppModule`, and `is3DMode = true`), and: draws the legend panel, distance rails, header and status bar of the **annotated figure R17 and the HLD §2 say is never rendered** — including a static `"source: v2x_relayed"` caption ([Canvas3DWarningView.kt:524](../../app/src/main/java/com/hackathon/v2x/ivi/ui/view/Canvas3DWarningView.kt#L524)) that asserts provenance as decoration; places all three vehicles at fixed canvas anchors, ignoring the message's actual positions ([Canvas3DWarningView.kt:142-147](../../app/src/main/java/com/hackathon/v2x/ivi/ui/view/Canvas3DWarningView.kt#L142-L147)); implements the guard as log-and-hide with no `[? UNKNOWN SOURCE]` marker, so an untrusted C on the default screen is indistinguishable from no C. `Scene3DProjection` — the actual projection math — is tested by 8 tests and called by **no production code**. `HomeScreen` is benign Display-Area content but a path §4 does not designate. |

### Findings that cross several rows

- **The runtime and the test suite disagree about what renders.** Hilt binds `Canvas3DWarningView` as the seam; the integration test asserts that binding; the composable ignores both and picks a renderer from local state. Whichever is intended, one of the three is wrong today.
- **Two Phase 5 plans, a second HLD and a second deploy procedure now live on `main`.** The PR merged `plans/phase5_team_master_plan.md`, `phase5_bach_tasks.md`, `phase5_tasks_for_teammate.md` and `phase5_vinh_progress.md` beside the authoritative `phase5_minh_tasks.md`; `IVI_ECU/doc/phase5-ivi-ecu-hld.md` (+ its two `.puml` twins) beside `ivi-ecu-hld.md`; and `IVI_ECU/deployment/phase5-ivi-deploy.md` beside [deploy-ivi-hmi-walkthrough.md](../../../requirements/car-sky-guide/deploy-ivi-hmi-walkthrough.md), which CLAUDE.md names authoritative for exactly this procedure. Both `deployment/` documents link `plans/phase5_tasks.md`, which does not exist on `main`, and the completion report still describes port 5004 and the `warningType` rewrite — the state this same branch later fixed.
- **`deployment/` is not a sanctioned location.** [node-code-layout.md § Per-folder doc/](../../../.claude/rules/node-code-layout.md) puts a node's report-style documents in `doc/`; procedures belong to the walkthrough. `phase5_completion_report.md` is additionally a progress record, which the documentation rules keep out of the repo.
- **Cross-cutting edits rode along:** the shared `verify-arm64-image` action now falls back from digest to mutable tag on pull failure (`3c0e518`) — trading away the "the thing verified is the thing pushed" guarantee its own comments state — and `.vscode/settings.json` hardcodes one developer's absolute JDK paths.

---

## 2 · Low-level review

**Variant used: requirements, not task IDs — the commits' IDs do not resolve against the authoritative plan.** Of the ~63 commits the merge introduced, ten carry `[X.Y.Z.W]` tags in five distinct IDs: `4.5.1.0`–`4.5.1.5`, `16.5.2.4`, `16.5.2.5`, `16.5.4.1`, `17.5.4.2`. Checked against [phase5_minh_tasks.md](../../../plans/phase5_minh_tasks.md) (named authoritative by [milestone1_high_level_plan.md § Phase 5](../../../documents/Plan-Proposal/milestone1_high_level_plan.md)): `4.5.1.1`–`4.5.1.4` collide with different work (v1 §2 documents the collision), `4.5.1.5` is the plan's *ProGuard keep rules* subtask while commit `3a650cd` used it for "save current progress before pulling IP changes", and `16.5.2.4`, `16.5.2.5`, `16.5.4.1`, `17.5.4.2` **exist in no plan file on `main` at all — including the branch's own merged plans**. Several IDs are also reused across commits (`16.5.2.5` three times, for a deck, a completion report and deploy instructions), against one-subtask-one-commit.

### Completion against requirements

Percentages are against each requirement's **acceptance** clause in [m1-cooperative-awareness.md §2](../../../requirements/m1-cooperative-awareness.md). Only requirements this PR touches are listed.

| Requirement | % complete | What is missing or deviated |
|---|---|---|
| **R3** — Ego object/track contract (IVI binding) | **60%** | Unchanged from v1: 6 of 9 `required` fields carry defaults in `R3Snapshot`, so a snapshot missing `class` or `state` decodes with fabricated values instead of failing. The mock that once forced this now sends every field — the defaults protect nothing and still mask producer drift. |
| **R4** — ADA → IVI warning message | **85%** | The acceptance clause — schema committed, round-trip tests, unknown `warningType` degrades gracefully — is now met on this side: preserved verbatim, rendered fail-safe, tested (`R4AdditiveVersionTest`, `R4DeserializerTest`). Remaining deviations are design-level rather than acceptance-level: `schemaVersion` unread, and the `state` half undecodable from the only in-folder producer. |
| **R5** — ECU deployment onto CarSky nodes | **45%** | The APK now has a launcher activity, so the clause *"the team APK launches on the AAOS node"* is finally satisfiable — but no launch on the node is recorded anywhere in the repo, so it is unverified, not done. The walkthrough's CI route exists (`ivi-assemble`, artifact `app-debug-apk`); no run ID is recorded. |
| **R6** — Inter-ECU network | **40%** | The port disagreement is resolved: 47300 on both this listener and `main`'s ADA sender, matching the blueprint. UDP reachability in a deployed Room — the clause itself — remains undemonstrated. |
| **R16** — HMI on the AAOS node | **80%** | The layout, the Display-Area switch on an R4 warning, and the button areas all work in code and under test (`MainViewModelTest`, `FullStackIntegrationTest`). Unverified on the AAOS node itself. The fabricated idle scene and always-`BOUND` status bar are the deviations that would mislead the person doing that verification. |
| **R17** — Warning view: God view of the 3 vehicles | **65%** | The 2D deliverable exists with a live provenance guard, but it is not what shows: the default is the unsanctioned 3D view, which draws the annotated variant R17 excludes ("no legend, no distance labels, no text overlay, no banner — the scene alone is the warning"), fixes vehicle positions to canvas anchors instead of drawing the message's geometry, mounts the banner, and — combined with `defaultMockScene` — can show "ghost C from `v2x_relayed`" with **zero messages received**, which inverts the clause's meaning. |
| **R18** — Evidence logging (IVI share) | **25%** | `[RX]`, `[DROP]` and the 2D guard's ERROR now exist on `IVI_V2X` — a run's receive half is reconstructible from logcat for the first time. Not structured `key=value`, no `[LINK]`/`[UI]`, and the default renderer's guard logs on a tag the documented filter misses. |
| **R19** — End-to-end demo run | **10%** | The IVI-side prerequisite — a live guard fed a real snapshot — now exists, and that is the 10%. No run, no capture; and the ghost-C source check would today have to explain away a renderer that can fabricate the exact evidence the check looks for. |

### Commit inventory

Evidence for the variant choice and the discipline findings; ~63 commits total, of which six are merges and eleven are **duplicate re-commits** of v1's inventoried work under new SHAs (the `feat/phase5-ivi-hmi-complete` lineage merged at `12ee3b1`, so `2300321`…`f5357c7` repeat `78c7c43`…`f7f2f55` verbatim — the same work now exists twice in history). Notable commits, in topic groups:

| Group | Commits |
|---|---|
| **The four v1-defect fixes** | `7c48b1b` (truncation) · `8203714` (guard wiring) · `3cdbc6b` (warningType) · `6542211` (R4ServiceError) — all untagged, `fix(ivi):` format |
| **The wiring** | `79f6e66` **[16.5.4.1]** feat: wire full R4 service → UI stack with Hilt DI · `7520804` **[16.5.2.4]** feat: auto-switch Display Area · `06cb936` **[16.5.2.4]** feat: align HMI with main contract, default HomeView |
| **Tests** | `ab67f3a` **[16.5.4.1]** test: full-stack integration diagnostics · `d9ad460` **[17.5.4.2]** test: Phase 5 full acceptance test suite |
| **The 3D God View arc** | `910aff2` 3D HUD with toggle → `b262cdc` **render default 3D scene when idle** (the fabricated scene) → `bb51111` WarningView as default mode (later reverted to Home by `06cb936`) → `5ea669b` redesign "per ivi-god-view-warning-screen.svg" (the figure R17 says is never rendered) → `96981ce`, `53f192e`, `e3e168f` polish → `56bc897` **mount WarningBannerOverlay** |
| **Logging** | `14feee1` `[RX]`/`[DROP]` under `IVI_V2X` |
| **Fix-up chains** | `0dafd90` (committed merge-conflict markers repaired "CI fix"), `0a78011`, `0dd4af0`, `904871a`, `b454cf2`, `ebb16b3`, `513ea84`, `5ee8ef4`, `f93fc19`, `84aaeab`, `1d20656` (replay=1, undone by `9fc31aa`) — each a build or test broken by a prior commit, against the pass-build-before-done rule |
| **Documents and plans** | `85977e5`, `f00ad89`, `eb3c5a3` (the parallel plan files) · `306873b`/`d287bf3`/`723c7a8` all tagged **[16.5.2.5]** for three different artifacts, the last mixing a deck, a credit note and a Gradle change in one commit · `ff3608e` wiki · `06ef283`, `10d3205`, `908a219`, `7f4e409` |
| **Cross-cutting** | `3c0e518` fix(ci): verify-arm64-image tag fallback · `e502e34`, `6115ca5`, `8b75294` (prompt-record housekeeping) · `4ed1967` compileOnly test deps for a language server |

Ten of ~63 commits carry a task ID; none of the IDs bind to the authoritative plan. The project format is `[<taskID>] <type>: <subject>` for every commit; `fix(scope):`, `ci:`, `merge:` and bare `docs:` (outside research reports) are all off-format.

---

## 3 · Conclusion

The distance closed since v1 is real and worth stating plainly. The finding that gated everything — excellent pieces connected to nothing — is gone: the APK launches, the service starts and binds, the repository collects, the view-models drive the screen, and a datagram sent to 47300 ends as a rendered warning. Every one of v1's four sharpest code defects was fixed by a commit that names the defect, understands it, and fixes it the right way — the truncation reset, the preserved `warningType`, the de-serialized `R4ServiceError`, and the snapshot wiring that made the R19 guard real. The full-stack loopback test is the best test in the node: real UDP, real service, both view-models, and an assertion on the mode switch. Wake-on-warning with previous-mode restore and user override is exactly the behaviour the design asks for. That is a strong round of work.

Three themes are worth your attention now.

**The screen can say things the data never said, and that is the one thing this node must never do.** The fabricated idle scene carries `source="v2x_relayed"` and a default risk of `high`; the default renderer captions ghost C "source: v2x_relayed" as static text and draws vehicles at fixed anchors rather than at the message's positions; the status bar reads `BOUND` whether or not the socket bound; the banner announces a warning for the mock scene. Each of these was added to make the demo look alive — and together they mean a judge, a teammate, or you cannot tell a working system from a painting of one. The whole point of R19 is that ghost C on screen *proves* a relay happened; a screen that can show ghost C from nothing spends that proof. The provenance guard you wired in is genuinely load-bearing now — these additions are what it is load-bearing against.

**The seams exist, are tested, and the runtime walks around them.** The injected view seam is ignored by the composable that should consume it; `Scene3DProjection` is tested by eight tests and called by no production code; `serviceError` flows to a collector that does not exist; the state collector is an empty block. This is the same pattern v1 flagged in a new place: green tests over unwired parts. The check that catches it costs one question per component — *what constructs this, and what consumes its output?*

**`main` now carries two answers to most questions about this node.** Two HLDs, two Phase 5 plan systems, two deploy procedures, task IDs that exist in no plan, and a completion report describing code this same branch later fixed. Every future reader — including the next reviewer — has to re-derive which document is real before they can act, and the answer this review had to establish is: `ivi-ecu-hld.md`, `phase5_minh_tasks.md`, and the walkthrough. The design departures the branch made — Hilt, the single module, the 3D default — may each be defensible, but the way to make them real is to change the decision record, not to merge a second record beside it.

None of this diminishes the core achievement: the receive path is correct, connected, and honest about failures, and the HMI behaves the way the design says it should when a real message arrives. The system underneath is now good enough that the remaining work is mostly subtraction — removing what the screen invents, and removing the duplicate authorities — rather than construction. The parser was the hardest part and you got it right in v1's round; the wiring was the hardest part of this round and you got that right too.
