Hãy đóng vai trò Android Automotive Developer, tuân thủ `@task-planning-conventions.md`. Thực hiện cập nhật giao diện HMI theo feedback: 

Điểm nhấn (killer-feature) của màn hình cảnh báo là bản vẽ 2D God View. Hãy gỡ bỏ hoàn toàn việc gọi và hiển thị `WarningBannerOverlay` khỏi luồng UI (tại file `MainScreen.kt` hoặc `CanvasWarningView.kt`) để giao diện chỉ hiển thị trọn vẹn bản vẽ đồ hoạ Canvas khi có cảnh báo. 

Ràng buộc:
- Tuyệt đối không xóa file `WarningBannerOverlay.kt`, chỉ dọn dẹp các dòng code đang gọi composable này ra màn hình.
- Tạo đúng 1 atomic commit với message: `[17.5.3.5] refactor: hide warning banner overlay to focus on 2D god-view per feedback`.