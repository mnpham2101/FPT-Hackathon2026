# Prompt — Implement Display Area view switcher state machine

**Date:** 2026-07-25 **Requested by:** xuanbach

## Prompt text

> Hãy đóng vai trò Android Automotive Developer, đọc kỹ rule `@task-planning-conventions.md` và thực hiện subtask 16.5.2.3:
> 1. Tạo file `DisplayMode.kt` chứa sealed class: `WarningView`, `HomeView`, `AppsView`, `SettingsView`.
> 2. Tạo `MainViewModel.kt` quản lý `currentMode: StateFlow<DisplayMode>`. 
> 3. Cập nhật `MainScreen.kt` để inject `MainViewModel`, gắn sự kiện onClick cho các nút ở SideButtonBar để gọi setMode().
> 4. Dùng `AnimatedContent` (fade 200ms) hiển thị nội dung theo `currentMode`. 
> 5. Ràng buộc an toàn: Trong hàm `setMode()`, chặn không cho đổi màn hình nếu `currentMode` đang là `WarningView`.
> 6. Tạo 1 atomic commit: `[16.5.2.3] feat: implement Display Area view switcher state machine`.

## Outcome

**Đánh giá: 10/10**
- Agent áp dụng cực kỳ tốt kỹ thuật State Hoisting (tách `MainScreen` stateful và `MainScreenContent` stateless) để dễ test và Preview không bị lỗi ViewModel.
- Tự động bổ sung dependency `lifecycle-viewmodel-compose:2.8.6`.
- Xử lý mượt mà AnimatedContent fadeIn/fadeOut. Ràng buộc WarningView được thực thi chặt chẽ.
- Commit sinh ra đúng chuẩn: `[16.5.2.3] feat: implement Display Area view switcher state machine` [4].