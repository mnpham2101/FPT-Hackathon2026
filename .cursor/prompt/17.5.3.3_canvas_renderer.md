# Prompt — Implement 2D Compose Canvas God View renderer

**Date:** 2026-07-25 **Requested by:** xuanbach

## Prompt text

> Hãy đóng vai trò Android Automotive Developer, tuân thủ `@task-planning-conventions.md` và thực hiện subtask 17.5.3.3 với mục tiêu duy nhất:
> 
> 1. Tạo file `app/src/main/java/com/hackathon/v2x/ivi/ui/view/CanvasWarningView.kt` implement interface `IviWarningViewSeam`.
> 2. Sử dụng Compose `Canvas` để vẽ God View 2D dựa vào `SceneCoordinateMapper.mapScene()` vừa tạo.
> 3. **Quy định Styling (Bắt buộc):**
>    - Nền canvas (Background fill): Màu Dark slate `#0D0D1A`.
>    - Đường vạch đứt tâm đường (Road centerline): Dọc từ trên xuống dưới, đứt nét, màu `#333355`.
>    - **Ego A:** Hình tròn đặc màu Cyan `#00D4FF` (bán kính 24px), có một tam giác nhỏ chỉ hướng về phía trước, kèm nhãn chữ "EGO" ở dưới.
>    - **Vehicle B:** Hình tròn đặc màu Amber `#FFB300` (bán kính 20px), có tam giác chỉ hướng, kèm nhãn "B".
>    - **Ghost C (Cực kỳ quan trọng):** Nếu `vehicleC != null`, vẽ đường viền đứt nét (dashed outline) màu Đỏ `#FF4040` (bán kính 20px). Nếu `vehicleC == null` thì không vẽ và không được crash.
>    - **Đường nối & Khoảng cách:** Vẽ vạch nối đứt nét mỏng `#555577` giữa (Ego - B) và (B - C). Vẽ nhãn text khoảng cách ở giữa vạch nối: `d_AB = X.X m` và `d_AC ≈ X.X m` (Dùng font Roboto Mono 12sp).
> 4. Không dùng `drawBitmap` hay load ảnh ngoài, chỉ dùng lệnh vẽ Canvas thuần. 
> 5. Tạo một hàm `@Preview` hiển thị thử với dữ liệu giả định: Ego(0,0), B(20,0), C(35,0).
> 6. Kiểm tra code và tạo 1 atomic commit với message: `[17.5.3.3] feat: implement 2D Compose Canvas God View renderer`.

## Outcome

**Đánh giá: 10/10**
- Implement hoàn chỉnh pipeline vẽ Compose Canvas 2D theo đúng thứ tự: nền → centerline → connectors → markers.
- Tuân thủ tuyệt đối single objective: chưa đụng tới glow hay badge, chỉ nhận `riskState` vào hàm. Xử lý triệt để null-safety cho `vehicleC`.
- Tối ưu cực tốt: Dùng `TextMeasurer` để vẽ chữ, format dấu chấm khoảng cách ổn định bằng `Locale.US`, thuần tuý gọi draw API (không load asset ngoài).
- Commit sinh ra chuẩn xác: `56315ef` với nội dung `[17.5.3.3] feat: implement 2D Compose Canvas God View renderer`.