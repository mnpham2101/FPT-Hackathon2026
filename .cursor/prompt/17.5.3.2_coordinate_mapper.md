# Prompt — Implement ego-frame to canvas coordinate mapper (pure Kotlin)

**Date:** 2026-07-25 **Requested by:** xuanbach

## Prompt text

> Hãy đóng vai trò là Android Automotive Developer, tuân thủ nghiêm ngặt `@task-planning-conventions.md` và thực hiện subtask 17.5.3.2 với mục tiêu duy nhất:
> 
> 1. Tạo file `app/src/main/java/com/hackathon/v2x/ivi/ui/view/SceneCoordinateMapper.kt` chứa một `object SceneCoordinateMapper` thuần Kotlin. Tuyệt đối không import bất kỳ thư viện `android.*` hoặc `androidx.*` nào.
> 2. Để tránh dính đến Compose `Offset`, hãy tự định nghĩa một `data class PixelOffset(val x: Float, val y: Float)` và `data class VehicleRenderData(val offset: PixelOffset, val radiusPx: Float)` ngay trong file này.
> 3. Viết hàm ánh xạ tọa độ nhận vào `canvasWidthPx`, `canvasHeightPx`, `scaleMetersPerPixel = 0.5f`, và thông tin xe.
> 4. Logic toán học bắt buộc:
>    - Ego (xe của mình) luôn được neo cố định ở tọa độ màn hình: `x = canvasWidthPx / 2` và `y = canvasHeightPx * 0.75f`.
>    - Tọa độ thực tế (mét): `x` dương là hướng về phía trước (trừ Y trên canvas), `y` dương là hướng sang phải (cộng X trên canvas).
>    - Phải có logic clamp giới hạn tọa độ `PixelOffset` không bị văng ra khỏi khung canvas, giữ lại lề 16px.
> 5. Xác nhận code không có lỗi bằng lệnh compile, sau đó tạo đúng 1 atomic commit với message: `[17.5.3.2] feat: implement ego-frame to canvas coordinate mapper (pure Kotlin)`.

## Outcome

**Đánh giá: 10/10**
- File `SceneCoordinateMapper.kt` thuần Kotlin tuyệt đối, chỉ import `SceneGeometry/VehiclePosition` từ package model, không có bất kỳ thư viện `android.*` hay `androidx.*` nào.
- Logic toán học `mapVehicle()` cực kỳ chuẩn: quy đổi `canvasX = anchorX + y/scale` và `canvasY = anchorY - x/scale`. Map trúng hoàn toàn 3 case nghiệm thu (B ở 20m phía trước, B ở 5m bên phải...).
- Tự động dùng `coerceIn` để chặn viền (clamp) tọa độ giới hạn 16px. Các biến cấu hình được tách thành hằng số công khai (DEFAULT_SCALE_METERS_PER_PIXEL, EDGE_MARGIN_PX...).
- Thêm hàm helper `mapScene()` rất thông minh để xử lý an toàn case `vehicleC = null`.
- Code hoàn toàn unit-testable mà không cần Robolectric.
- Commit chuẩn xác: `[17.5.3.2] feat: implement ego-frame to canvas coordinate mapper (pure Kotlin)`.