# Prompt — Build R16 AAOS main screen layout scaffold in Jetpack Compose

**Date:** 2026-07-25 **Requested by:** xuanbach
## Prompt text

> Hãy đóng vai trò là một Android Automotive Developer, dùng `@task-planning-conventions.md` và thực hiện subtask 16.5.2.2:
> 1. Tạo file `MainScreen.kt` chứa top-level `@Composable`.
> 2. Bố cục sử dụng `BoxWithConstraints` (1280x720). `DisplayArea` ở giữa chiếm 70% chiều rộng; `SideButtonBar` trái/phải chứa Home, Apps, Settings; và `BottomNavBar`.
> 3. Tông màu Dark Automotive: nền #1A1A2E, nhấn #00D4FF, chữ #E8E8F0. Font Roboto Mono / Roboto.
> 4. Ràng buộc: Không dùng pixel cứng, mọi kích thước dùng `dp`. Vùng bấm button ≥ 48dp.
> 5. Tạo 1 atomic commit: `[16.5.2.2] feat: build R16 AAOS main screen layout scaffold in Jetpack Compose`.

## Outcome

**Đánh giá: 10/10**
- Agent xuất sắc tự động cấu hình thêm plugin Compose BOM 2024.09.03 để tương thích `compileSdk 34`.
- Sử dụng Slot API cho `DisplayArea` chuẩn kiến trúc mở rộng. Tối ưu icon lưới mà không cần import thư viện nặng.
- Preview render chính xác tỷ lệ màn hình xe hơi (landscape 1280x720).
- Commit sinh ra đúng chuẩn: `[16.5.2.2] feat: build R16 AAOS main screen layout scaffold i