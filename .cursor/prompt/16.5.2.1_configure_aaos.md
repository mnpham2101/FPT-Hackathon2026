# Prompt — Configure AAOS Gradle module, manifest, and network security

**Date:** 2026-07-25 **Requested by:** xuanbach

## Prompt text

> Hãy đóng vai trò là một Android Automotive Developer và thực hiện subtask 16.5.2.1 với mục tiêu duy nhất (single objective):
> 1. Thiết lập module Gradle cho AAOS với cấu hình compileSdk = 34, targetSdk = 33, minSdk = 29. Cấu hình plugin Hilt và kotlinx.serialization.
> 2. Khai báo AndroidManifest.xml với <uses-feature android:hardware.type.automotive required="true"/> và các quyền INTERNET, FOREGROUND_SERVICE.
> 3. Cấu hình network_security_config.xml cho phép cleartextTraffic.
> 4. Tạo đúng 1 atomic commit với message chính xác là: `[16.5.2.1] chore: configure AAOS Gradle module, manifest, and network security`.

## Outcome

**Đánh giá: 10/10**
- Agent tự động nhận diện repo chưa có project Android, tự khởi tạo toàn bộ Gradle module từ đầu cực kỳ chuẩn xác.
- Vượt qua lỗi xung đột phiên bản AGP (8.13.0) và Hilt sau khi được prompt bổ sung. 
- Lệnh `./gradlew assembleDebug` chạy thành công (Build Successful) thông qua Android Studio (vượt qua rào cản môi trường Java 25 của máy gốc).
- Commit sinh ra đúng chuẩn: `[16.5.2.1] chore: configure AAOS Gradle module, manifest, and network security` [2].