# Prompt — Define IviWarningViewSeam rendering interface

**Date:** 2026-07-25 **Requested by:** xuanbach

## Prompt text

> Hãy đóng vai trò là một Android Automotive Developer, tuân thủ nghiêm ngặt các quy tắc chia task (atomic commit, single objective) và thực hiện subtask 17.5.3.1 với các yêu cầu sau:
> 
> 1. Tạo file `app/src/main/java/com/hackathon/v2x/ivi/ui/view/IviWarningViewSeam.kt`.
> 2. Trong file này, định nghĩa một `interface IviWarningViewSeam` chứa một hàm `@Composable fun Render(scene: SceneGeometry, riskState: String)`.
> 3. Vì Task Group 5.1 chưa được thực hiện, hãy tạo tạm một `data class SceneGeometry` chứa vị trí tương đối tính bằng mét (x: Float, y: Float) của 3 đối tượng: `ego`, `vehicleB`, và `vehicleC` (lưu ý `vehicleC` phải có thể null vì ban đầu xe C chưa bị phát hiện).
> 4. Ràng buộc quan trọng: Không được import các framework Android UI tĩnh (zero Android UI framework imports) vào file interface này, chỉ sử dụng các annotation của Compose.
> 5. Đảm bảo code không có lỗi syntax (compile thành công) để hàm `Render` có thể chấp nhận `vehicleC = null` mà không bị crash.
> 6. Tự động tạo đúng 1 atomic commit với message chính xác là: `[17.5.3.1] design: define IviWarningViewSeam rendering interface`.

## Outcome

**Đánh giá: 10/10**

- Created `app/src/main/java/com/hackathon/v2x/ivi/ui/view/IviWarningViewSeam.kt` chứa interface `IviWarningViewSeam` và `data class SceneGeometry`.
- Đảm bảo đúng ràng buộc kiến trúc: không import bất kỳ thư viện UI framework tĩnh nào của Android, chỉ dùng Compose thuần.
- Chấp nhận `vehicleC = null` an toàn.
- Đã tạo thành công 1 atomic commit đúng format dự án: `[17.5.3.1] design: define IviWarning