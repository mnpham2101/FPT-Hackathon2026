# Prompt — Add Ghost C V2X badge, risk glow, and defensive source guard

**Date:** 2026-07-25 **Requested by:** xuanbach

## Prompt text

> Hãy đóng vai trò Android Automotive Developer, tuân thủ `@task-planning-conventions.md` và thực hiện subtask 17.5.3.4 với mục tiêu duy nhất:
> 
> 1. Cập nhật file `CanvasWarningView.kt` để nâng cấp hiển thị cho Ghost C.
> 2. **Hiệu ứng vòng sáng (Risk Glow):** Thêm một vòng sáng bao quanh Ghost C, đập nhịp (pulsing) bằng `InfiniteTransition` với alpha từ `0.3` đến `0.8`, chu kỳ `1.2s`. Màu của vòng sáng phụ thuộc vào `riskState`: "low" → Amber `#FFB300`, "medium" → Orange `#FF6600`, "high" → Red `#FF1A1A`. 
> 3. **Nhãn thông tin (V2X Badge):** Vẽ một badge nhỏ nằm phía trên Ghost C với nội dung dạng `[V2X] C · 28.3 m · RISK: HIGH`. Sử dụng font Roboto Mono 11sp, nền badge màu `#1A1A2E` (alpha 0.85), bo góc `6dp`.
> 4. **Defensive Source Guard (Bắt buộc):** Thêm kiểm tra logic - nếu nguồn dữ liệu của `vehicleC` (thông qua `R3Snapshot.source`) KHÁC `"v2x_relayed"`, tuyệt đối KHÔNG render Ghost C bình thường. Thay vào đó, vẽ một hình tròn màu vàng `#FFD700` với dấu chấm hỏi và nhãn `[? UNKNOWN SOURCE]`, đồng thời ghi log ERROR kèm toàn bộ chuỗi JSON của snapshot.
> 5. Chú ý tối ưu: Hiệu ứng animation chỉ nên dùng `drawBehind` hoặc được đọc bên trong DrawScope để không kích hoạt recomposition toàn bộ layout.
> 6. Kiểm tra code không lỗi và tạo đúng 1 atomic commit với message: `[17.5.3.4] feat: add Ghost C V2X badge, risk glow, and defensive source guard`.

## Outcome

**Đánh giá: 10/10**
- Hiệu ứng đập nhịp (Glow) được xử lý cực tốt bằng `InfiniteTransition`. Tối ưu xuất sắc khi chỉ đọc `glowAlpha` trong draw phase (lambda `drawBehind`) để hoàn toàn vô hiệu hóa recomposition/relayout thừa.
- Defensive Source Guard được implement hoàn hảo: tạo model tạm `R3Snapshot.kt`, bảo vệ UI bằng cờ `v2x_relayed`, render icon dấu hỏi vàng `[? UNKNOWN SOURCE]` an toàn và ghi log JSON một lần duy nhất với `LaunchedEffect`. Xử lý fail-safe cho `riskState` lạ rất chặt chẽ.
- V2X badge hiển thị chuẩn thiết kế kỹ thuật (Roboto Mono 11sp, bo góc 6dp, nền alpha).
- Các Preview được tổ chức thông minh để bao quát cả case "trusted" (mock) và "unknown source" (guard active).
- Commit chuẩn xác: `97e6dea` với message `[17.5.3.4] feat: add Ghost C V2X badge, risk glow, and defensive source guard`.