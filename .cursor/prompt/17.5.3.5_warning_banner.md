# Prompt — Add warning alert banner with risk color and countdown timer

**Date:** 2026-07-25 **Requested by:** xuanbach

## Prompt text

> Hãy đóng vai trò Android Automotive Developer, tuân thủ `@task-planning-conventions.md` và thực hiện subtask 17.5.3.5 với mục tiêu duy nhất:
> 
> 1. Tạo file `app/src/main/java/com/hackathon/v2x/ivi/ui/view/WarningBannerOverlay.kt`.
> 2. Implement một component overlay (sử dụng `Box` đè lên trên `CanvasWarningView`) hiển thị một banner cảnh báo trải dài theo chiều ngang màn hình.
> 3. **Nội dung banner:** Hiển thị dòng chữ "⚠ NLOS OBSTRUCTION — Vehicle C ahead (relayed via V2X)".
> 4. **Countdown Timer:** Thêm một thanh `LinearProgressIndicator` depleting (cạn dần) theo thời gian timeout của biến cấu hình `WARNING_TIMEOUT_MS`.
> 5. **Màu sắc (Risk Color):** Nền của banner là dải màu bán trong suốt (semi-transparent) khớp với mức độ `riskState` (giống logic màu của risk glow: low -> Amber, medium -> Orange, high -> Red).
> 6. **Nút Dismiss (X):** Có một nút [X] để đóng banner. **Ràng buộc UI quan trọng:** Khi người dùng bấm [X], banner biến mất ngay lập tức nhưng tuyệt đối KHÔNG được clear `CanvasWarningView` (màn hình Canvas cảnh báo xe hơi vẫn phải tiếp tục hiển thị bình thường).
> 7. **Lắng nghe ViewModel:** Khi `uiWarningState` từ `WarningViewModel` chuyển sang `Idle`, banner tự động biến mất (thu hồi) cùng với Canvas.
> 8. Tạo các hàm `@Preview` hiển thị các trạng thái màu banner khác nhau.
> 9. Kiểm tra code compile thành công, sau đó tạo đúng 1 atomic commit với message: `[17.5.3.5] feat: add warning alert banner with risk color and countdown timer`.

## Outcome

**Đánh giá: 10/10**
- Banner được implement chuẩn xác bằng Box overlay đè lên trên CanvasWarningView, nút Dismiss [X] hoạt động hoàn hảo dựa trên state cục bộ mà không làm ảnh hưởng đến bản vẽ God View bên dưới.
- Tái sử dụng khéo léo hàm ánh xạ màu từ Canvas (đổi thành `internal riskColor`) giúp đồng bộ màu sắc giữa Banner và Glow mà không lặp code (DRY).
- Tuân thủ tuyệt đối Single Objective: Hoist `warningActive` state thay vì gọi ViewModel (việc nối ViewModel thuộc subtask 16.5.4.1).
- Countdown timer sử dụng `Animatable` với giá trị timeout được externalize thông minh ra `BuildConfig.WARNING_TIMEOUT_MS` thông qua file Gradle, chuẩn bị sẵn đường cho logic thật ở 4.5.1.4.
- Commit sinh ra chuẩn xác: `bc2f138` với nội dung `[17.5.3.5] feat: add warning alert banner with risk color and countdown timer`.