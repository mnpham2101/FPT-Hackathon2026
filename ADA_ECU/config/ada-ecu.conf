# Canonical CarSky runtime defaults. Environment variables use the uppercase form.
gate_enter_m=30.0
gate_exit_m=35.0
track_timeout_ms=1000
confirm_hits=3
event_log_path=ada-events.jsonl
v2x_listen_host=0.0.0.0
v2x_listen_port=47200
r2_receive_timeout_ms=200
ivi_host=10.99.0.13
ivi_port=47300
detector_enabled=true
detector_cmd=python3 /app/detector/tools/video_detector.py --video /app/media/ego-b-occluding-c.mp4 --backend yolo-onnx --model /app/models/yolo11n.onnx --every-n-frames 4 --confidence 0.20 --realtime --loop
detector_restart_max=3
cra_enabled=nlos_obstruction
risk_near_m=50.0
risk_critical_m=30.0
risk_dwell_ms=300

# Backward-compatible aliases, if injected, have lower precedence than canonical names:
# MISS_LIMIT_MS, TENTATIVE_HITS, LOG_PATH, ADA_LISTEN_HOST/PORT, IVI_ECU_HOST/PORT.
